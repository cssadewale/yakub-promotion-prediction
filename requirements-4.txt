# ─────────────────────────────────────────────────────────────
# Yakub Trading Group — Promotion Prediction App
# Streamlit Deployment Dependencies
#
# IMPORTANT: These versions are pinned to EXACTLY match the
# environment in which the model was trained (Google Colab).
# Do NOT upgrade these versions — doing so will break the
# model loading step with an AttributeError.
#
# Trained on:
#   Python       3.12.13
#   scikit-learn 1.6.1
#   numpy        2.0.2
#   joblib       1.5.3
# ─────────────────────────────────────────────────────────────

# Core data handling — pinned to training versions
numpy==2.0.2
pandas>=2.0.0

# Machine learning — MUST match training version exactly
scikit-learn==1.6.1

# Model loading — MUST match training version exactly
joblib==1.5.3

# Web application framework
streamlit>=1.32.0

# Google Drive model download (model exceeds GitHub's 25 MB limit)
gdown>=4.7.0
